// vim: ft=c et sw=4

int printf(char *, ...);

int main() {
    char *str = "abcd";
    for (char *p = str; *p; p++) {
        *p = *p + 1;
    }
    // > bcde
    printf("%s\n", str);
    return 0;
}
